# docker-compose 配置本服务的本地容器运行方式，方便联调和独立启动。
services:
  face-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vision-recognition-service
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload